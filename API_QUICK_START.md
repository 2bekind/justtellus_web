# Быстрый старт API для мини-приложения

## 📋 Пошаговая инструкция

### Шаг 1: Установка зависимостей

```bash
pip install flask flask-cors
```

Или:

```bash
pip install -r requirements.txt
```

### Шаг 2: Запуск

**Вариант A: Запуск всего сразу (рекомендуется)**

Дважды кликните на `start_all.bat` - запустятся и бот, и API сервер.

**Вариант B: Запуск отдельно**

1. Запустите бота: `python bot.py` (или `start_bot.bat`)
2. В новом терминале запустите API: `python api_server.py`

API сервер будет доступен на: `http://localhost:5000`

### Шаг 3: Настройка URL в HTML

Откройте `profile.html` и найдите строку 201:

```javascript
const API_URL = 'http://localhost:5000'; // ИЗМЕНИТЕ НА ВАШ АДРЕС
```

**Для локальной разработки:** оставьте как есть

**Для продакшена:** замените на ваш домен (например: `https://api.ваш-сайт.com`)

### Шаг 4: Проверка работы

1. Убедитесь, что API сервер запущен
2. Откройте в браузере: `http://localhost:5000/api/stats/8109892353`
3. Должен вернуться JSON с данными

### Шаг 5: Развертывание на хостинге

См. подробную инструкцию в `API_INSTRUCTIONS.md`

## 🔧 Что делает API?

- **GET `/api/stats/<user_id>`** - возвращает статистику пользователя
- **GET `/api/health`** - проверка работоспособности

## ⚠️ Важно

- API сервер должен работать постоянно (как и бот)
- Для локальной разработки используйте `localhost:5000`
- Для продакшена нужен реальный домен с HTTPS

## 📝 Пример ответа API

```json
{
  "success": true,
  "owner": true,
  "stats": {
    "received": 10,
    "sent": 5,
    "choosers": 3,
    "max_streak": 7
  }
}
```

